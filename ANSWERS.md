1. What is the DOM?
the DOM is the Document Object model
it is essentially a tree structure where elements have parents and children that relate to each other with tree functionality



2. What is an event?
and event is a DOM approved change that can be interacted with using javascript or other languages

3. What is an event listener?
and event listener relates to events in that a user can perform and it is waiting for those events to be performed. Event listeners are usually attached to something on the DOM or element


4. What is a Constructor
a constructor is a special method within a class. it initializes the object with pre determined values and functionality. 